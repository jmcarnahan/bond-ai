# Frontend resources have been merged into the combined backend service.
# See backend.tf for the single App Runner service that serves both
# the Flutter frontend (via Nginx) and FastAPI backend (via Uvicorn).
